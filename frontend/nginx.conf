server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # This is the magic line that makes React Router work in production!
        try_files $uri $uri/ /index.html;
    }
}